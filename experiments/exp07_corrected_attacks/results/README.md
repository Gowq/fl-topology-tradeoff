# Exp. 07 corrected results

One atomic JSON is written per configuration. Generated result JSONs are not
versioned. `analysis_exp07_corrected.json` is produced only after completeness
validation or explicitly with `--allow-partial`.

