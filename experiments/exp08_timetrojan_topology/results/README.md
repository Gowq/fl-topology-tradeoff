# Exp. 08 Results

This directory stores JSON outputs from `run_exp08.py` and the generated
`analysis_exp08_timetrojan.json` report. Attack artifacts are stored separately
under `experiments/exp08_timetrojan_topology/artifacts/` because they are reused
by multiple jobs and their SHA-256 hashes are part of the validity checks.
