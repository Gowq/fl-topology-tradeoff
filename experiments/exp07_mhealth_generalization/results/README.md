# Exp. 07 results

This directory intentionally contains no claimed full-run results yet.

`tune_exp07.py` writes `tuned_hyperparameters.json`; `run_exp07.py` then writes
one atomic JSON file per stable protocol configuration. Commit result artifacts
only after the complete five-seed matrix and integrity checks finish.
