# Runtime packages that are not provided by the PyTorch Docker base image.
# Keep torch/torchvision in the base image so CUDA, cuDNN and wheel versions
# remain coherent on GPU clusters.
opacus>=1.6,<1.7
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65
